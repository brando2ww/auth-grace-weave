

## Plano: Filtro de status com "disponivel" como padrão

Alterar o estado inicial do filtro de status em `src/pages/Estoque.tsx`:

- Mudar `useState("todos")` para `useState("disponivel")` na variável `statusFilter`.

### Arquivo alterado:
- `src/pages/Estoque.tsx` -- Uma linha: valor inicial do `statusFilter`.

